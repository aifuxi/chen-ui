import { ArrowRight } from "lucide-react"

import { ComponentPreview } from "@/components/docs/component-preview"
import { DocHeader } from "@/components/docs/doc-header"
import { InstallCommand } from "@/components/docs/install-command"
import { Button } from "@/components/ui/button"
import {
  Dialog,
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
import { Input } from "@/components/ui/input"
import { getSourceFilePath } from "@/lib/docs"

export const metadata = {
  title: "Dialog",
  description: "Chen Serif dialog component documentation.",
}

<DocHeader
  category="Components"
  title="Dialog"
  description="Modal dialog primitives for confirmations, focused editing flows, and lightweight layered tasks."
  sourcePath={getSourceFilePath("dialog")}
/>

<InstallCommand name="dialog" />

## Usage

```tsx
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"

export function Example() {
  return (
    <Dialog>
      <DialogTrigger render={<Button variant="outline" />}>Open dialog</DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Invite collaborator</DialogTitle>
          <DialogDescription>
            Layer a focused task without leaving the current page.
          </DialogDescription>
        </DialogHeader>
      </DialogContent>
    </Dialog>
  )
}
```

## Example

<ComponentPreview
  title="Invite flow"
  description="DialogContent includes the backdrop and centered popup surface."
  className="flex flex-wrap gap-3"
>
  <Dialog>
    <DialogTrigger render={<Button variant="outline" />}>Open invite dialog</DialogTrigger>
    <DialogContent>
      <DialogHeader>
        <DialogTitle>Invite collaborator</DialogTitle>
        <DialogDescription>
          Send access to a teammate without leaving the documentation context.
        </DialogDescription>
      </DialogHeader>
      <div className="flex flex-col gap-3">
        <Input placeholder="name@example.com" type="email" />
        <Input placeholder="Optional note" />
      </div>
      <DialogFooter>
        <DialogClose render={<Button variant="ghost" />}>Cancel</DialogClose>
        <Button>
          Send invite
          <ArrowRight data-icon="inline-end" />
        </Button>
      </DialogFooter>
    </DialogContent>
  </Dialog>
</ComponentPreview>

## Notes

- Always include `DialogTitle` for accessibility.
- Use `DialogClose` when you need a dismiss action rendered as a custom button.
